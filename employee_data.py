class EmployeeDATA:
    def __init__(self, fullname='no data', post='no data', birth_year='no data', payment='no data'):
        self.fullname = fullname
        self.post = post
        self.birth_year = birth_year
        self.payment = payment
